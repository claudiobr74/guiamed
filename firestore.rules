rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Acesso direto pelo cliente é negado.
    // O Next.js usa o Admin SDK no servidor e filtra por organizationId da sessão.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
